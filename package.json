{
  "name": "game-of-districts-istanbul",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix backend run generate && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=20"
  }
}